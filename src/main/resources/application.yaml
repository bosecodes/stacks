spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stacks
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org:
      springframework:
        security: DEBUG

# jwt settings
jwt:
  secret: secretKey
  expiration: 3600000 # 1 hour in milliseconds